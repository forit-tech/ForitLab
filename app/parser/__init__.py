"""Web Parser — ядро парсер-машины Forit Lab.

Продуктовый инвариант: массовый сбор данных с сайтов
(сайт → что собрать → лучший источник → обход → результат → экспорт),
а не HTTP-инспектор. Технические возможности живут в режиме Explore/Advanced.

Субфаза 1a: базовые модели, определение ввода (URL/HTML/JSON/cURL), определение
типа источника, унифицированный DOM за абстракцией (backend lxml → stdlib),
и единственный рабочий поток INPUT → INSPECT. Сеть — только через app.net.

Логика существующего Scrape (parser/entities/discovery) переиспользуется, но
пока не удаляется — миграция в 1f после доказанной parity.
"""

TOOL_ID = "parser"
TOOL_TITLE = "Web Parser"
TOOL_SUMMARY = (
    "Парсер-машина: вставь сайт/HTML/JSON/cURL — Lab определит источник данных и "
    "поможет собрать их в таблицу. Режимы Extract / Crawl / Audit / Explore."
)
